# Imágenes de la app

Imágenes importadas en el código (favicon, logo del encabezado, etc.). Vite las incluye en el bundle con hash para cache.

- **logo_loeram_favicon.png** → favicon (se inyecta en `main.tsx`)
- **logo_loeram_short.png** → logo del encabezado (se usa en `App.tsx`)
